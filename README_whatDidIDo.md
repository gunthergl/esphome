# Setup the environment
```bash
git clone git@github.com:gunthergl/esphome.git
cd esphome
./script/setup   # Created the "venv" virtual environment
# There was a VSCode popup asking to select the interpreter from the "venv" folder, please do so.


# Copy esphome/components/max7219 to esphome/components/sevenseg
# Replace *.h and *.cpp with sevenseg.h .cpp


```
